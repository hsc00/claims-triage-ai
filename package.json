{
  "name": "sentinel-triage-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "eval": "tsx src/eval.ts",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"const fs=require('fs');const path=require('path');const dir=path.join(process.cwd(),'logs');if(fs.existsSync(dir)){fs.rmSync(dir,{recursive:true,force:true});console.log('Cleaned logs/');}else{console.log('logs/ already absent');}\""
  },
  "dependencies": {
    "openai": "^4.68.0",
    "zod": "^3.23.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
